# Supabase (projeto → Settings → API)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Opcional mas recomendado: pedidos do /mesa sem erro de RLS ao devolver a linha
# Settings → API → service_role (nunca exponha no cliente / Git)
SUPABASE_SERVICE_ROLE_KEY=
